"""
Optional OCR prototype.

The original version used OCR to extract personal identity information and write
it to Profile.json. That workflow is intentionally excluded from the public demo
because it can process sensitive personal data.

If reintroduced later, keep all identity documents local, never commit them,
and store secrets outside source control.
"""
